# Program 4 · Phase D-2A — never commit real values, state, or credentials.
terraform.tfvars
*.auto.tfvars
*.tfstate
*.tfstate.*
# Saved plan artifacts (binary, environment-specific, often credentialed) — never commit.
*.tfplan
plan.tfplan
.terraform/
.terraform.lock.hcl
*-key.json
*.json.key
crash.log
